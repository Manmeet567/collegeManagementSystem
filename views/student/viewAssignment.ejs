<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student | View Assignment</title>
    <style>
        img{
            max-height: 80vh;
            max-width: 100%;
        }
    </style>
</head>
<body>
    
    <h2>Title : <%= assignment.title %></h2>
    <% if( assignment.desc ) { %>
        <p>Description : <%= assignment.desc %></p>
    <% }else{ %>
        <p>Description : No description added.</p>
    <% } %>
    <img src="data:assignment/<%=assignment.img.contentType %>;base64, <%=assignment.img.data.toString('base64')%>" alt="Image not found">
    
    <p>Uploaded By : <%= assignment.author %></p>
    <p>Uploaded For : <%= assignment.forBranch %> <%= assignment.forSem %></p>
</body>
</html>